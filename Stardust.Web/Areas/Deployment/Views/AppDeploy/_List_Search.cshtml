@using NewLife;
@using NewLife.Web;
@using NewLife.Cube;
@using XCode;
@using Stardust.Data.Deployment;
@{
    var fact = ViewBag.Factory as IEntityFactory;
    var page = ViewBag.Page as Pager;
}
@* @await Html.PartialAsync("_SelectApp", new SelectAppModel { Id = "appId", AppId = page["appId"].ToInt() }) *@
<div class="form-group">
    <label for="category" class="control-label">类别：</label>
    @Html.ForDropDownList("category", AppDeploy.GetCategoryList(), page["category"], "全部", true)
</div>
@await Html.PartialAsync("_Enable")
@await Html.PartialAsync("_DateRange")